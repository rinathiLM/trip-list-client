{{#if trips}}
  <table class="table table-hover header-fixed">
    <thead>
      <tr>
        <th class="column-1">Location</th>
        <th class="column-2">Have you gone there?</th>
        <th class="column-3">Edit</th>
      </tr>
    </thead>
    <tbody>
      {{#each trips as |trip|}}
        <tr>
          <td>{{trip.location}}</td>
          <td>{{trip.done}}</td>
          <!-- link to update and delete each hike in the last column -->
          <td>
            <button class="btn btn-default" data-id="{{trip.id}}" data-toggle="modal" data-target="#update-trip-modal{{trip.id}}"><span class="glyphicon glyphicon-pencil"></span>
            </button>
            <button class="btn btn-default" data-id="{{trip.id}}" data-toggle="modal" data-target="#delete-trip-modal{{trip.id}}"><span class="glyphicon glyphicon-trash"></span>
            </button>
          </td>
        </tr>
      {{/each}}
    </tbody>
{{else}}
  <p>It doesn't look like you have any trips.</p>
  </table>
{{/if}}

<!-- MODALS -->
{{#each trips as |trip|}}

<!-- update trip -->

<!-- delete trip warning -->
<div class="modal fade" id="delete-trip-modal{{trip.id}}" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
      </div>
      <div class="modal-body">
        <p>Are you sure you want to delete this trip?</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="delete-trip btn btn-danger" data-id="{{trip.id}}" class="btn btn-primary">Delete</button>
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
      </div>
    </div>
  </div>
</div>

{{/each}}
